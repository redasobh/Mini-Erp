# Phase 3 — Frontend & POS UI Plan

بناء الواجهة الأمامية ونظام نقطة البيع (POS) باللغة العربية لنظام **Mini ERP Restaurant System** بالاعتماد الكامل على البنية التحتية وقاعدة البيانات المعتمدة في المرحلتين الأولى والثانية (Supabase + PostgreSQL).

---

## 1. المحددات الأساسية (Core Constraints)
1. **عدم المساس بقاعدة البيانات:** الباك إند والجداول والدوال الذرية (`complete_sale` و `process_return`) وسياسات الـ RLS هي المصدر الوحيد للحقيقة (Source of Truth).
2. **اللغة العربية واتجاه اليمين لليسار (RTL):** الواجهة مصممة بالكامل لتكون طبيعية وسلسة لموظفي المطعم باللغة العربية.
3. **عدم إضافة ميزات غير مطلوبة:** تم استبعاد أي ميزات خارج نطاق الـ MVP (مثل الباركود، الكيتشن ديسبلاي، الطاولات، الموردين، إلخ).
4. **الاعتمادية والعمليات الذرية:** عمليات البيع والإرجاع تستدعي دوال الباك إند الذرية مباشرة دون تجزئة في الواجهة.

---

## 2. هيكل شاشات ومكونات الواجهة (Architecture & Modules)

```text
src/
├── lib/
│   ├── supabaseClient.ts          # عميل Supabase المعتمد
│   └── utils.ts                   # دوال مساعدة للعملة والتاريخ بالعربية
├── types/
│   ├── database.ts                # أنواع قاعدة البيانات المعتمدة
│   └── app.ts                     # أنواع السلة وواجهة المستخدم
├── context/
│   ├── AuthContext.tsx            # إدارة الجلسة والدور (Admin / Agent)
│   └── ToastContext.tsx           # إشعارات النجاح والأخطاء
├── components/
│   ├── layout/
│   │   ├── AppLayout.tsx          # الإطار العام
│   │   ├── Sidebar.tsx            # القائمة الجانبية المفلترة حسب الدور
│   │   └── Header.tsx             # الترويسة العلوية مع الساعة وبيانات المستخدم
│   ├── common/
│   │   ├── Modal.tsx              # النوافذ المنبثقة
│   │   ├── ConfirmDialog.tsx      # تأكيد العمليات الحساسة
│   │   ├── EmptyState.tsx         # الحالات الفارغة بالعربية
│   │   └── LoadingSpinner.tsx     # مؤشر التحميل
│   └── invoice/
│       └── InvoiceModal.tsx       # عرض وطباعة الفاتورة الحرارية
└── views/
    ├── LoginView.tsx              # شاشة تسجيل الدخول
    ├── DashboardView.tsx          # لوحة المؤشرات التشغيلية
    ├── POSView.tsx                # شاشة الكاشير ونقطة البيع الرئيسية
    ├── SalesView.tsx              # سجل الفواتير السابقة
    ├── ReturnsView.tsx            # شاشة معالجة المرتجعات الذرية
    ├── ProductsView.tsx           # إدارة المنتجات (للمدير)
    ├── CategoriesView.tsx         # إدارة التصنيفات (للمدير)
    ├── InventoryView.tsx          # مراقبة المخزون وتنبيهات النواقص
    └── ReportsView.tsx            # تقارير المبيعات والمخزون
```

---

## 3. خطة التنفيذ المعتمدة (Implementation Order)
1. تثبيت حزم `lucide-react`, `tailwindcss`, `postcss`, `autoprefixer`.
2. إعداد Tailwind CSS ودعم RTL والطباعة `@media print`.
3. إنشاء سياق المصادقة `AuthContext` وسياق التنبيهات `ToastContext`.
4. إنشاء الهيكل العام (Layout, Sidebar, Header).
5. بناء شاشات الكاشير (POS) ونوافذ الدفع والفاتورة المطبوعة.
6. بناء شاشة المرتجعات واستدعاء دالة `process_return` الذرية.
7. بناء شاشات إدارة المنتجات، التصنيفات، المخزون، والتقارير.
8. اختبار البناء `npm run build` والتحقق الشامل من الشاشات.
